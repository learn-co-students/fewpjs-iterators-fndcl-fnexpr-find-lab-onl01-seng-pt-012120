// const testVar = {}

// function testFunc() {
//   return "hi"
// }

function superbowlWin(record){
  let answer;
  for (let object in record) {
    // object.find( function(e) { return e.result === "W"})
    if (object[result] === "W"){
      answer = object;
    }
  }
  return answer[year];
}